@using SHotel.Core.EnumForCore
@using SHotel.Core.Models
@model List<Order>

    @{
        int counter = 1;
    }
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

<div class="container" style="margin-top:200px;margin-bottom:100px; border: 1px solid #000; min-height:50vh;">
    <h1>Orders: </h1>

    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Order Id</th>
                <th scope="col">Total Amount</th>
                <th scope="col">Created Date</th>
                <th scope="col">Status</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
                <tr>
                    <th scope="row">@(counter++)</th>
                    <td>@item.Id</td>
                    <td>@item.TotalPrice  $</td> 
                    <td>@item.CreatedDate.ToString("MMM dd, yyyy H:mm:ss")</td>
                    <td>
                        <span class="badge text-white text-bg-@(item.OrderStatus == OrderStatus.Pending ?
                "secondary" : item.OrderStatus == OrderStatus.Accepted ? "success" : "danger")" style="padding: 0.4rem 0.8rem;
            font-size:16px ">@item.OrderStatus</span>
                    </td>

                </tr>
            }
        </tbody>
    </table>
</div>

@* <p><span>@item.DayCount days </span> <b>$ @(itemAmount = ((item.Room.Price - item.Room.Price * item.Room.DiscountPercent / 100) * item.DayCount))</b></p>
<span hidden @(totalAmount += itemAmount)></span> *@

